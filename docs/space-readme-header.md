<!--
Canonical YAML header for the paperQA Hugging Face Space's README.md.
This file exists in the GitHub repo only as a reference — it is NOT the
Space's README. Copy the block below (including the --- fences) to the
top of the Space's README.md when first creating the Space, and keep it
in sync when the gradio version or metadata changes.

WHY separate from the GitHub README: GitHub renders raw YAML as ugly code;
Hugging Face Spaces uses the YAML frontmatter to configure the Space.
Keeping two headers would split the source of truth — instead, the GitHub
README has no frontmatter and this file documents the Space-only one.
-->

```yaml
---
title: paperQA
emoji: 📄
colorFrom: blue
colorTo: indigo
sdk: gradio
sdk_version: "4.44.0"
app_file: app.py
pinned: false
---
```

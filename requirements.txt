# HF Spaces installs from this file at build time.
#
# WHY this file exists alongside pyproject.toml: Spaces does not read
# pyproject optional-dependencies — it installs whatever is in
# requirements.txt. Keep this list aligned with the [app] + [embed] + [llm]
# extras in pyproject.toml; when a runtime dep changes there, update here
# in the same commit.

# Note: paperqa/ lives at the repo root (not src/paperqa/), so it lands on
# sys.path automatically when the Space runs `python app.py` from /app.
# No "pip install ." needed — and HF's Dockerfile mounts requirements.txt
# at /tmp anyway, which would resolve "." to the wrong directory.

# Core
numpy>=1.26,<3
pypdf>=4,<7

# Embedder (all-MiniLM-L6-v2)
sentence-transformers>=3,<5

# HF Inference API backend
huggingface-hub>=0.24,<1

# UI — pinned range matches sdk_version in README.md frontmatter.
gradio>=5,<7
